name: Build Prism Swap APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Unzip project
        run: |
          if [ -f cordova-project.zip ]; then
            unzip -o cordova-project.zip -d .
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK Platform 36 + Build Tools
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Setup Gradle 8.14.2
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.14.2

      - name: Install Cordova CLI
        run: npm install -g cordova

      - name: Install project dependencies
        working-directory: cordova-project
        run: npm install

      - name: Add Android platform
        working-directory: cordova-project
        run: cordova platform add android@15.0.0

      - name: Build APK (debug) - WITHOUT Tapsell plugin (test)
        working-directory: cordova-project
        run: cordova build android --debug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: prism-swap-debug-apk-NOTAPSELL-TEST
          path: cordova-project/platforms/android/app/build/outputs/apk/debug/*.apk
